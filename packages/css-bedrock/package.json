{
  "name": "zd-css-bedrock",
  "version": "0.3.1",
  "private": true,
  "main": "dist/index.css",
  "style": "src/index.css",
  "dependencies": {
    "normalize.css": "^5.0.0",
    "zd-css-fonts": "git+ssh://git@github.com/zendeskgarden/css-fonts.git#0.2.0"
  },
  "devDependencies": {
    "cssnano": "^3.8.0",
    "gh-pages": "^0.11.0",
    "live-server": "^1.1.0",
    "postcss-cli": "^2.6.0",
    "postcss-cssnext": "^2.8.0",
    "postcss-import": "^8.1.2",
    "pre-commit": "^1.1.3",
    "stylelint": "^7.5.0",
    "stylelint-config-zendesk": "git+ssh://git@github.com/zendeskgarden/stylelint-config.git#4.0.0",
    "zd-css-variables": "git+ssh://git@github.com/zendeskgarden/css-variables.git#0.9.0"
  },
  "scripts": {
    "build": "postcss --config .postcss.json",
    "lint": "stylelint $(find src -name '*.css')",
    "nuke": "rm -rf node_modules",
    "publish": "gh-pages -d demo",
    "rebuild": "npm run nuke && npm install && npm run build",
    "start": "npm run watch & live-server demo",
    "test": "npm run lint",
    "watch": "npm run build -- --watch"
  },
  "pre-commit": [
    "build",
    "test"
  ]
}
