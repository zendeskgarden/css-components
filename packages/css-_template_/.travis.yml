language: node_js
node_js:
  - 7

sudo: false

cache:
  directories:
    - node_modules

deploy:
  provider: pages
  github_token: $GITHUB_TOKEN
  local_dir: demo
  skip_cleanup: true
  on:
    branch: master
