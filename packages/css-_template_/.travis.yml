language: node_js
node_js:
  - 7

sudo: false

cache:
  directories:
    - node_modules

before_deploy: "cat ~/.npmrc"

deploy:
  provider: npm
  email: $ARTIFACTORY_EMAIL
  api_key: $ARTIFACTORY_API_KEY
  on:
    all_branches: true
    # branch: master
    # tags: true
