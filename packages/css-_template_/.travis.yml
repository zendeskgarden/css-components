language: node_js
node_js:
  - 7

sudo: false

cache:
  directories:
    - node_modules

before_deploy:
  - git config user.email "Travis CI"
  - git config user.name "travis@travis-ci.com"

deploy:
  provider: script
  script: npm run publish
  skip_cleanup: true
  on:
    branch: master
