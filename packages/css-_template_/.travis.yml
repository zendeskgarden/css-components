language: node_js
node_js:
  - 7

sudo: false

cache:
  directories:
    - node_modules

deploy:
  provider: npm
  email: $ARTIFACTORY_EMAIL
  api_key: $ARTIFACTORY_API_KEY
  on:
    all_branches: true
    # branch: master
    # tags: true

after_deploy: "cat ~/.npmrc"
